language: objective-c
before_script: 
  - export LANG=en_US.UTF-8
  - cd Example
  - pod --version
  - pod install
script: "xctool -workspace HTPressableButton.workspace -scheme HTPressableButtonTests -sdk iphonesimulator build test"
